# SPDX-License-Identifier: AGPL-3.0-or-later
.svelte-kit
build
build-docs
node_modules
playwright-report
test-results
.pw-data
# Generated by cmd/tsenums (kept byte-stable for the Go drift check; do not reformat).
src/lib/enums.generated.ts
# Demo build inputs produced by `make wasm demo-seed` (vendored/generated, gitignored).
# Prettier runs from web/, so it reads web/.gitignore (empty) + this file, NOT the repo
# root .gitignore that ignores these — hence they must be listed here too, or prettier
# tries to parse them (a ~500MB *.wasm binary crashes it with "Invalid string length").
static/*.wasm
static/wasm_exec.js
static/demo-seed.json
static/demo-users.json
