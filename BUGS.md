# intraktible — Known Issues & Bugs

Tracked alongside `PLAN.md`; updated in the same PR at the end of every phase.
Format: `ID | severity | component | description | status`.

## Open — audit round 3 (planned; sequenced into PRs, see PLAN.md roadmap)
A third audit (code/security, a UI/UX review against screenshots of every page × persona × theme, and a competitive + API study vs. comparable decisioning and BPMN/DMN platforms) produced the backlog below. Grouped into healthy-sized PRs (one open at a time; no anemic PRs).

PR1 — data protection + log usability (DONE, PR #6)
- `A1 | HIGH | decision-engine | the per-node decision trace was recorded UNSEALED while the decide input/output were sealed, so node outputs echoing PII survived a crypto-shred erasure — node outputs are now sealed via sealPII before emit and unsealed (or "[erased]") in maskRecord; regression test TestNodeTraceErasure. | fixed — PR #6`
- `A2 | MED | web | Decisions list: history.ListPage adds flow/env/status/variant + decision-id search + time range + pagination (limit/offset, total; limit<=0 = unpaginated for the dashboard/SDK); the page gained filter controls, a pager, and absolute-time-on-hover. | fixed — PR #6`
- `A3 | MED | web/audit | Audit log: ReadPage adds offset+total + an exclude_type param; the page gained a date range, a "Hide node steps" toggle, a pager, and absolute timestamps; CSV exports the whole filtered set. | fixed — PR #6`

PR2 — engine builder UX (DONE, PR #7)
- `A4 | MED | web/engine | the canvas is now the primary surface, floated to the top (CSS order), with the operational panels behind a tab bar (Test & analyze · Deploy & versions · Monitors · Discussion) instead of one ~3,200px scroll. | fixed — PR #7`
- `A5 | LOW | web/engine | canvas polish — Svelte Flow attribution hidden (proOptions), taller default (560px), "Relax"→"Auto-layout", descriptive toggle tooltips. | fixed — PR #7`
- `A6 | LOW | web/engine | the test-run input shows a live "not valid JSON" hint (Run disabled while invalid) and a "Sample input" button that prefills a skeleton from the flow input_schema. | fixed — PR #7`
- `A7 | LOW | web/engine | Promote is now a primary button with a confirm() step (wording differs for the production four-eyes path). | fixed — PR #7`
- `A8 | LOW | web/engine | flow-create fields are labelled (Slug/Name) and the deployment columns show a "not deployed" pill instead of a bare "—". | fixed — PR #7`

PR3 — decision explainability + case management (DONE, PR #8)
- `A9 | MED | web | the decision trace now surfaces the decisive split branch as a badge (→ yes/no) alongside the already-shown reason codes. Per-node duration is intentionally NOT recorded (domain.Execute is the pure/deterministic core — no per-node wall clock; total duration is shown). Matched rule/row index for rule/decision_table nodes is a future deterministic-safe domain enhancement (out of scope here). | fixed — PR #8`
- `A10 | MED | web/cases | the queue has row checkboxes + a select-all, and a bulk bar to assign selected cases or mark them completed (confirm on complete); selection clears on reload. | fixed — PR #8`
- `A11 | MED | web/cases | case detail groups Actions vs Activity and renders the audit as a timeline (markers + relative/absolute time + actor). | fixed — PR #8`
- `A12 | LOW | web/cases | the open-case form inputs are labelled (Company/Type/SLA days); the SLA-sweep tooltip already existed. | fixed — PR #8`

PR4 — accessibility + visual consistency
- `A13 | HIGH | web | --fg-subtle darkened in both themes (light #8a929e→#6b7280, dark #6b7480→#828c99) so secondary text clears WCAG AA 4.5:1. | fixed — PR #9`
- `A14 | MED | web | placeholder-as-label replaced with real <label>s on the worst offenders (agents define form grouped Identity/Behavior; data feature form). Status badges already render their text (e.g. decisions <span class="badge {status}">{status}</span>), so status is not color-alone. | fixed — PR #9`
- `A15 | MED | web | one "account & view" menu now carries identity (Signed in as / Not signed in), the view-as persona switcher, and sign-in/out — the competing top-bar controls are gone; /login renders minimal chrome (brand + theme only, no Primary nav or account control). e2e (login.spec) asserts both states. | fixed — PR #9`
- `A16 | LOW | web | contrast tokens unified app-wide (A13), persona labels render verbatim, and a base h1 rule applies each persona's editorial type tokens (--h1-family/size/weight/spacing) so inner-page headings adopt the showcase hierarchy. The deepest editorial carry-through (full per-page spacing rework) remains a subjective, ongoing polish item. | mostly fixed — PR #16`
- `A17 | LOW | web | done: agents-form grouping + data feature-form labels/preview (PR #9); a shared Breadcrumb on the decision + case detail pages; a plain-language policy band preview (in-order bands + default); the command palette placeholder already names its scopes; ≥24px hit targets on the builder's small icon (✕) buttons (WCAG 2.5.8). | fixed — PR #16`

PR5 — robustness & bug-fix round
- `A18 | MED | eventlog/nats | Read now takes both bounds from one StreamInfo (no separate Head() RPC to race) and clamps fromSeq up to FirstSeq; only a gap strictly inside [first,last] is corruption. Test asserts past-head clamps empty + tail read. | fixed — PR #10`
- `A19 | MED | agent-manager | StartRun's full-queue fallback now runs process() in a tracked goroutine (h.wg), so StartRun returns immediately (202 contract) and shutdown still drains it. Test fills the queue and asserts prompt return + completion. | fixed — PR #10`
- `A20 | MED | agent-manager | EscalateRun resolves a run via the tenant-scoped agents.GetRun projection (O(1)); on a projection miss it falls back to a tenant-scoped log fold for read-after-write. Unknown-run test added. | fixed — PR #10`
- `A21 | MED | eventlog | the poll path now reads a bounded batch (readFrom with LIMIT on sqlite/postgres; public Read stays unbounded for replay), re-arms on a full batch, and runs on a stop-tied context cancelled on Close. Internal tests cover both. | fixed — PR #10`
- `A22 | MED | platform/kms | GCP KMS now sets + verifies CRC32C (request VerifiedPlaintextCrc32C + response checksums) per Google's data-integrity guidance, erroring on mismatch; the client is an interface so a fake exercises the integrity paths. | fixed — PR #10`
- `A23 | LOW | store | UpdateDoc wraps the read-modify-write in a transaction on a TxStore (direct path inside an existing Tx / the in-memory store); the SQLite store now serializes writers with a mutex (so a concurrent write tx waits rather than tripping SQLITE_LOCKED) and applies pragmas pool-wide via the DSN. Concurrent-update test added. | fixed — PR #10`
- `A24 | LOW | context-layer | the sqlite connector DSN is now hardened: forced read-only (mode=ro), in-memory rejected, and — when ITK_SQL_CONNECTOR_DIR is set — confined to that directory (the long-deferred L2). Unit tests cover read-only/in-memory/allowlist. | fixed — PR #10`
- `A25 | LOW | auth | Keyring.Resolve is now a direct map[hash] lookup (O(1), constant in keyring size) — the lookup key is already a fixed-width hash, so the prior linear constant-time scan only leaked the keyring size. | fixed — PR #10`
- `A26 | LOW | scim | userNameFilter now extracts the quoted value between the first and last quote (handles values with spaces, case-insensitive attr/op), so the deprovisioning gate no longer misses a user. Table-driven unit test added. | fixed — PR #10`
- `A27 | LOW | decision-engine/export | BPMN now assigns ids via a per-export uniqueness map (collisions suffixed _2,_3…), and element/flow/DI refs all read from it so distinct node ids never alias. Collision test added. | fixed — PR #10`
- `A28 | MED | web/policies | the CommentThread is now keyed on the policy id so switching policies remounts and reloads its comments. Fixed an incidental null-deref (versions can be null for an unpublished policy) surfaced by the new switch-reload e2e. | fixed — PR #10`
- `A29 | MED | web/agents | the name-change effect now closeStream()s and clears the stale streaming output before reloading, so a live stream is torn down on sibling navigation rather than leaking the socket and polluting the new page. | fixed — PR #10`
- `A30 | LOW | web/engine | nodeTelemetry is now cleared (clearTelemetry) on every structure/config edit (add/delete node, update config, add/connect/delete edge), matching the "cleared on edits" contract. | fixed — PR #10`
- `A31 | LOW | web | BuilderDeck's recent-sort now uses localeCompare (returns 0 for equal timestamps), a consistent total order instead of the prior never-zero comparator. | fixed — PR #10`

PR6 — decision-table hit policies + aggregators
- `A32 | MED | decision-engine | decision_table now resolves rows under a DMN hit policy (config "hit": first|unique|any|rule_order|collect, with "aggregate" sum|min|max|count for collect). UNIQUE/ANY detect conflicts and fail loudly; rule_order/collect gather per-target values (rules independent). The deprecated "mode":"all" still works. Surfaced in the builder (hit-policy + aggregate selects) and the OpenAPI config prose; assertions need no change (they match the flow output map). Domain + builder e2e tests added. | fixed — PR #11`
- `A33 | LOW | docs | docs/EXPRESSIONS.md documents expr-lang (conditions/expressions) + Starlark (code node) as a stable, versioned contract, incl. the D9 decision NOT to add a second/standard engine; linked from the decision-engine README and the OpenAPI config prose (also corrected from "CEL-like" to expr-lang). | fixed — PR #11`

PR7 — external decision API (compatibility surface)
- `A34 | — | — | PREREQ resolved by decision: the live contract is auth-gated and not fetched; we implemented the neutral functional shapes from the backlog (array-of-rows decide already existed at /decide/batch; per-flow OpenAPI added). No external docs/branding copied; competitor never named in-repo. | done — PR #12`
- `A35 | MED | platform/openapi | the array-of-rows decide endpoint already existed (/v1/flows/{slug}/{env}/decide/batch); added the missing per-flow generated OpenAPI 3.1 contract at GET /v1/flows/{slug}/openapi.json (openapi.ForFlow embeds the flow's latest published input schema as the request data schema). Documented in the static spec. Generator + endpoint e2e tests added. | fixed — PR #12`
- `A36 | MED | auth | API-key Scope gained a wildcard ("*" = any environment) + trailing-"/*" prefix patterns via Scope.Allows/ValidScope, and the decide/batch/preapprove endpoints now ENFORCE it (403 on a non-matching environment) — the first scope enforcement (previously stamped but never checked). Dev + test-harness keys set to "*" to preserve behaviour. Allows/ValidScope unit tests + a decide-enforcement e2e added. | fixed — PR #12`
- `A37 | LOW | decision-engine | /v1/decisions accepts start_time/end_time (RFC3339; aliases of since/until) and include_node_results (default true; set false to omit the heavy per-node trace from list rows). Documented in the spec. | fixed — PR #12`

PR8 — ML model hosting (epic; needs a product decision)
- `A38 | — | decision-engine | the models-as-data slice landed (PR #15): a model registry (POST/GET /v1/models, command→ModelDefined→projection→ModelView) + a Predict node, evaluated in a pure, deterministic decision-engine/models package (logistic | gbm tree-ensemble | expr-lang expression). The shell resolves+evaluates a Predict node from the registry and injects predict.<output> (recorded for replay — mirrors Connect/AI). Builder: a /models registry page + a predict node panel; OpenAPI + docs updated. | fixed — PR #15`
- `A38c | — | decision-engine | model-drift monitoring: predictions are tagged with their model name (predict.<output>.model) and a DriftProjector folds Predict-node outputs into a per-model probability-decile histogram (ModelStats). GET /v1/models/{name}/drift returns the distribution + PSI vs a captured baseline (POST .../baseline); <0.1 stable / 0.1–0.25 moderate / >0.25 significant. Pure PSI/bucket unit-tested; an end-to-end test proves a post-baseline distribution shift yields PSI>0. The /models page shows a per-model drift readout (histogram + PSI badge + capture-baseline). | fixed — PR #16`
- `A38d | — | decision-engine | windowed drift + a model-PSI monitor: ModelStats keeps per-day histograms (pruned to 90d) so GET /v1/models/{name}/drift?window=Nd measures only the recent N days (a shift the cumulative view dilutes); 0/absent = all-time. POST /v1/models/{name}/monitor {threshold} sets a PSI alert and the drift report's "firing" flag trips when PSI exceeds it. Windowed-drift + monitor-firing unit tests + a command→projector→threshold test; the /models drift readout gained a window selector, a firing badge, and a threshold control. | fixed — PR #17`
- `A38b | — | decision-engine | ML phase 2 — BYO external model serving: a fourth model kind "external" {endpoint,timeout_ms} where the shell POSTs the features to an operator-stood-up HTTP serving endpoint (egress-guarded via connectors.EgressPolicy.Client, injected as an HTTPDoer so models stays decoupled) and reads a {score,probability} response, recorded for replay like the in-process kinds. models page + OpenAPI + README updated. | fixed — PR #16`
- `A39 | LOW | context-layer | connector catalog broadened from 6 to 18 templates (income/employment verification, open banking, bank-account verify, device/IP risk, email/phone risk, address verification, KYB, watchlist/sanctions, geo-IP, collateral valuation, a SQL feature-store lookup) — data scaffolds over the existing http/sql connectors; a well-formedness test guards the catalog. | fixed — PR #16`
- `A39b | LOW | context-layer | two real connector adapters beyond the http/sql scaffolds: graphql (POSTs {query,variables} — the decide input becomes the variables — egress-guarded, fails loudly on GraphQL errors) and static (serves fixed JSON verbatim: constants/thresholds/feature flags, or stubbing an integration). Both selectable in the data page + in the catalog (graphql, static-flags templates). Tested (graphql happy + errors-fail-loudly, static). | fixed — PR #17`
- `A40 | LOW | web | authoring AI-copilot: POST /v1/copilot/explain {graph} returns a plain-language explanation of a flow, and POST /v1/copilot/suggest {prompt} turns a natural-language requirement into a suggested node breakdown. Backed by an injected AICompleter port wired to ai.Registry (decoupled — engine never imports the AI provider); 503 when unconfigured. A "Copilot" tab in the builder (explain this flow / suggest logic). Go e2e (fake completer + unconfigured-503) + a builder e2e via the Stub provider. | fixed — PR #16`
- `A40b | LOW | web | deeper copilot — graph generation: POST /v1/copilot/generate {prompt} asks the AI for a STRUCTURED flow graph (CompleteJSON over a graph JSON-schema) and validates it server-side (domain.ValidateGraph) before returning, so the builder only ever applies a graph the engine accepts (422 otherwise). The Copilot tab gains "Generate & apply a flow" which applies the validated graph to the canvas via importJSON for review-then-publish. Go e2e (valid→200, unusable→422) + a builder e2e (Stub→graceful 422). | fixed — PR #17`
- `LR1 | — | platform | launch-readiness sweep: a global JSON request-body cap (httpx.DecodeJSON → 8 MiB MaxBytesReader; the NDJSON stream path is exempt by design), a GET /version endpoint (VCS revision + Go toolchain, unauthenticated like /healthz), and docs/LAUNCH.md (an operational checklist: durability, security toggles, health/introspection, observability, the gate). Tests for the body cap + version handler. | fixed — PR #17`
- `A41 | LOW | api | large-job batch path: POST /v1/flows/{slug}/{env}/decide/stream takes NDJSON (one input per line) and streams NDJSON results one per line — constant memory, no row cap, scope-enforced, each row a real recorded decision. A dependency-light alternative to a gRPC/Arrow wire that delivers the same "very large jobs" capability (full gRPC/Arrow remains a future option if a binary wire is needed). OpenAPI + README + e2e. | fixed — PR #16`
- `LR2 | — | decision-engine | scheduled model-drift push: a models.Scheduler sweeps every tenant's model stats (ListDocs over decision_model_stats) and pushes the ok→firing PSI edge to webhooks via notify.Notifier, deduping on a steady-firing model via new decision.model.drift_alerted/_resolved events (ModelStats.Alerting, surfaced as DriftReport.alerting + a "📤 alert pushed" badge). Mirrors the flow monitor.Scheduler: started in main on INTRAKTIBLE_MONITOR_INTERVAL (one cadence drives both sweeps); INTRAKTIBLE_MODEL_DRIFT_WINDOW narrows the firing window (default cumulative). Unit tests (firing edge/dedup/skip-unmonitored/nil-notifier), a projector alert/resolve test, and a full e2e (real Scheduler + Notifier + DriftProjector + webhook sink: predict→baseline→shift→deliver-once-then-dedup). | fixed — PR #18`
- `LR3 | — | context-layer | first-class provider connectors + define-time validation: http/graphql gain an auth block (bearer | header | basic | query) + custom headers; plaid (creds injected into the request body, env-selected base URL) and stripe (bearer secret key, GET retrieval) are dedicated adapters. All credential fields are sealed by the keyring + masked in the redacted view. connectors.ValidateConfig is called at define time (service.defineConnector) so a malformed endpoint/credential fails on save (400) instead of deferring to the first fetch — across ALL types. Catalog + data-page dropdown updated. Tests: http/graphql auth + sealing (black-box), plaid/stripe Fetch + auth-config validation (white-box). | fixed — PR #18`
- `LR4 | — | platform | launch dry-run against Postgres: booted the binary with --store/--log=postgres (INTRAKTIBLE_POSTGRES_DSN), walked docs/LAUNCH.md end to end (healthz/version/openapi/auth/connector define+validate+redaction/catalog/model define+monitor+drift/webhook subscribe; both schedulers tick clean), and ran the INTRAKTIBLE_TEST_POSTGRES store + eventlog contract tests green. LAUNCH.md corrected (make ci is the full gate, not make check) + expanded (connector auth/validation, the dual scheduler + drift window). | done — PR #18`

PR9 — persona-adaptive UI (epic; key differentiator) + API-first
- `A42 | HIGH | web | personas are no longer skins: a persona is now a config-driven composition (lib/persona.ts PersonaConfig) that changes navigation (an ordered, relabelled subset of the shared catalog), the default home, and the surfaced primary actions — plus the existing accent/type/density. Each role sees different sections, terminology, and landing, not one re-skinned layout. | fixed — PR #13`
- `A43 | MED | web | expanded from 3 skins to a 7-role, config-driven, extensible set: Workflow Designer, Developer/Integrator, Risk Operator, Team Manager, Product/Experimentation, Executive, Evaluator/Guest — each declares nav/home/actions/terms; adding a persona is config + an accent block (no fork). | fixed — PR #13`
- `A44 | MED | web/api | the deep per-persona views landed (PR #14): Developer → an API-keys management page (/keys: list/create/rotate/revoke via /v1/api-keys, secret revealed once) + traces/API-reference links; Executive → ShowcaseDeck gained a decision-volume trend (decisionsByDay) + a governance tile (live flows / pending four-eyes); Evaluator → a guided 4-step tour (EvaluatorTour) over the live sandbox; Risk Operator keeps its OperatorDeck. The API reference is linked (server /docs + per-flow openapi.json) rather than embedded. | fixed — PR #14`
- `A45 | HIGH | platform/api | API-first guarantee documented (docs/API-FIRST.md) and ENFORCED by web/src/lib/api-first.test.ts: the audit confirmed every network call already routes through api.ts (/v1 only), there are no SvelteKit server routes, and the only UI-only state is the persona/theme local preference. The test fails any future UI-only data path. | fixed — PR #13`

## Closed by decision (won't implement)
- `D9 | decision-engine | CEL conditions — won't implement. expr-lang already serves Rule/Split conditions + Assignment, and Starlark serves the Code node, so conditions/expressions are fully covered. A second expression engine (cel-go) would add a dependency and parallel semantics for no new capability. Revisit only if a concrete need for CEL specifically arises.`
- `D11 | decision-engine | batching the per-node decision events — won't implement. The per-node DecisionStarted → NodeEvaluated… → Completed/Failed stream IS the replayable, node-by-node decision history (PLAN §3.3); batching would trade that granularity for throughput the MVP does not need. A future high-volume optimization, not a bug.`

## Fixed
- `D21b | store/projection | durable stores now RESUME from a checkpoint instead of fully rebuilding from offset 0 on every boot. The store gained an atomic transaction (store.Tx / store.TxStore, implemented by SQLite + Postgres; a Tx satisfies store.Store so projectors write through it unchanged); the projection runtime applies each event AND advances a checkpoint (_projection/applied_head) in one transaction, so the non-idempotent projectors (analytics counters, case audit/notes appends, run/event counters) are applied exactly once — no per-projector idempotency required. At boot it resumes from checkpoint+1 (transactional durable store with a valid checkpoint) or full-rebuilds (memory, or first boot); an in-memory applied-watermark also de-dups the boot-rebuild/live-bus overlap. Verified: a durable-store resume test proves no re-apply and no reset (a sentinel survives), and a real-binary --store=postgres restart resumed with the checkpoint row persisted. | fixed`
- `D21 | store | added a Postgres store.Store adapter (store.NewPostgres via jackc/pgx/v5 + pgxpool) behind the existing interface, selectable with serve --store=postgres (DSN from INTRAKTIBLE_POSTGRES_DSN) — the durable/shared option for large or multi-process projections. One docs(collection, key, doc JSONB) table, parameterized queries. VERIFIED against a real PostgreSQL 17 (not just written): the full Store-contract test (store_test.go TestPostgresStore, gated on INTRAKTIBLE_TEST_POSTGRES so the no-Postgres CI skips it) passes, and a real-binary --store=postgres smoke created a flow + read it back after a restart (projections persist). The deploy compose pg profile provides the database. Incremental resume-from-Head split out as D21b. | fixed`
- `D17 | agent-manager | agent runs can now run asynchronously, event-sourced. POST /v1/agents/{name}/run with {"async":true} records an AgentRunStarted (status "running") and queues the work, returning 202 + run_id immediately; a worker pool drains the queue, invokes the provider (with tool-calling), and records the terminal AgentRunRecorded; callers poll GET /v1/agent-runs/{run_id}. The projection folds RunStarted→running and RunRecorded→completed/failed. Crash/shutdown safety: workers process on a background context so a graceful shutdown lets in-flight runs finish (DrainWorkers bounds the wait before the log closes), and a run left "running" (started, no terminal) is re-enqueued at boot by RecoverRunning (folds the log, runs after the projections rebuild so the worker can resolve the agent). The synchronous path is unchanged. Verified: command tests (async completion, unknown-agent rejection, recovery re-enqueues + skips finished) under -race, and a real-binary async run (202→running→completed). (Token streaming landed afterwards — see TS12 — over SSE/WS; the run is still recorded as one terminal event so replay stays stable.) | fixed`
- `D1 | eventlog | the file WAL held every envelope in memory (memory grew with the log). It now keeps only a compact in-memory byte-offset index (one int64 per record) and streams records from disk on demand via ReadAt (a single read for the requested contiguous tail) — retained memory is O(events) offsets instead of O(events × envelope size), and open scans for record boundaries without decoding every record. Records are validated lazily on Read; a corrupt complete record fails loudly there, and the projection runtime Reads the whole log at boot so corruption still stops startup (the torn-final-write recovery from D22 is preserved). Trade-off: the existing per-command log folds (caseExists / runAgentName) now read from disk rather than an in-memory slice — they were already O(n) by design; the SQLite-backed log (D18) is the indexed/scalable backend for both memory and hot reads. | fixed`
- `D12 | case-manager | SLA breaches are now pushed, not only derived on read. A sweep (command.SweepSLA, exposed as POST /v1/cases/sla-sweep for a scheduler to call) folds the case stream, finds open cases past their deadline as of now, and emits a CaseSLABreached event for each not-yet-breached one. The breach is an effect computed against the wall clock and then recorded, so replay reads the recorded breaches and stays stable; the sweep is idempotent (a breached case is skipped, and the projector folds a re-emitted breach as a no-op). The cases projection marks sla_breached + audits it. Read-time days_left / sla_state remain (the stored projection is still clock-free). | fixed`
- `D10 | web | the builder now has structured config panels for ALL node types: the nested-table types (rule = when→then repeaters, scorecard = factor when/weight rows, decision_table = mode + row when→outputs repeaters, 2d_matrix = row/col condition lists + a cell grid of literal values) joined the flat-config panels (split/connect/ai/manual_review/output/code/assignment). The raw-JSON textarea stays as the per-type advanced view. The canvas also supports drag-to-connect (Svelte Flow onconnect → addUniqueEdge, deduplicated) alongside the from/to form. The repeaters update via index-matched .map / .at (no dynamic key writes, clean under eslint security/detect-object-injection); pure helpers are unit-tested and structured editing is covered by e2e. | fixed`
- `D18 | eventlog | the file WAL is single-process, so the split-services profile gave each module its own log. Added eventlog.SQLiteLog — a shared, durable, append-only log backed by one SQLite file (global AUTOINCREMENT seq, WAL mode + busy_timeout serialize cross-process writers) behind the existing Log interface, selectable with serve --log=sqlite. SQLite has no pub/sub, so a background poller delivers newly-committed rows (its own and other processes') to in-process subscribers; Read/Head are immediately consistent and live delivery is eventual (bounded by the poll interval) — a projection-lag, not data loss. The split compose profile now shares one volume with --log=sqlite --store=sqlite so cross-component flows work across processes. Verified: two handles on one dir share events + live-deliver across handles (unit), and the real binary rebuilds projections from the shared log after restart. The monolith file WAL stays the default. | fixed`
- `D16 | agent-manager | an agent's declared tools are now executed via a tool-calling loop. The ai.Provider boundary gained tool specs, tool calls, and a conversation history; the agents loop (InvokeWithTools, bounded by maxToolSteps) lets the model request tools, executes each via a Toolbox, feeds the results back, and records the full tool trace on the run (events.ToolCall + RunView.ToolCalls) so it is auditable and replay-stable. The reference Toolbox (agent-manager/tools.ConnectorToolbox) exposes Context Layer connectors as tools and is wired into both the Agent Manager and the engine AI node at the composition root. The OpenAI-compatible HTTP provider maps tools↔tool_calls (verified against a mock server); the Stub still answers directly (no tools). | fixed`
- `D14 | context-layer | added a SQL reference connector (connector type "sql") alongside http + mock_bureau. Config is {dsn, query, args[]}; it opens the configured database (the pure-Go modernc.org/sqlite driver, already a dependency — "sqlite" is the only built-in driver, others fail loudly), runs the query binding the declared args as named parameters (values, never string-interpolated — caller params cannot inject SQL), and returns {"rows":[...]} bounded to 1000 rows under the fetch timeout. Verified end to end against a temp sqlite db. Other drivers (Postgres/MySQL) become available in a build that imports them, behind the same Connector interface. | fixed`
- `D19 | decision-engine | the JSON-Schema validator (platform/schema, shared by decide-input contracts and agent structured output) was a flat object/required/per-property-type check; richer keywords were accepted but unenforced. It is now a recursive validator covering: type (incl. type arrays), required, properties, additionalProperties (bool or schema), min/maxProperties, enum, const, allOf/anyOf/oneOf/not, numeric minimum/maximum/exclusive bounds/multipleOf, string minLength/maxLength/pattern and formats (email/uuid/date/date-time), array items/minItems/maxItems/uniqueItems, and local $ref (a JSON pointer into the same document, e.g. #/$defs/X). Recursion is depth-bounded (fails loudly on a $ref cycle). Unknown keywords stay lenient. | fixed`
- `D15 | context-layer | the http connector dialed an operator-configured URL with no SSRF protection. It now enforces an egress policy at dial time (a net.Dialer Control hook that inspects the resolved IP, so it guards every redirect hop and resists DNS rebinding): loopback, RFC1918/ULA private, link-local, unspecified, and multicast targets are refused by default. Operators whose connectors legitimately reach internal hosts opt in with INTRAKTIBLE_CONNECTOR_ALLOW_PRIVATE (logged loudly at boot). connectors.InvokeWith / Provider.Egress / service.WithEgress thread the policy; the scheme + time + size bounds remain. | fixed`
- `D20 | web/auth | the UI has a login page (web/src/routes/login): an API key is exchanged for a session cookie, the home page shows the signed-in identity (via GET /v1/me) with sign-out, and sessions are durable (see the D7/storesessions work) so they survive a restart under --store=sqlite. The module pages still offer an API-key input as well — both auth methods work; a full migration of those pages to cookie-only is optional future polish. | fixed`
- `D13 | web | the case-detail page now renders the case context (the source decision's inputs / agent escalation reference) as a readable key-value view (lib/kv.displayEntries: primitives as text, nested values as compact JSON) instead of nothing. A fully schema-aware decision-inputs view remains a possible future polish. | fixed`
- `D17 (partial) | agent-manager | a schema-constrained agent's structured output is now validated against the agent's schema (shared platform/schema.ValidateObject, the same JSON-Schema subset the decide-input check uses); a mismatch is a recorded failed run (fail loudly). Remaining (async/queued/streaming runs) stays open as D17. | fixed`
- `D5 | ai | added a real OpenAI-compatible HTTP provider (ai.NewHTTP — POST {base}/chat/completions, Bearer auth, json_object response_format when a schema is requested) behind the existing ai.Provider interface. It works with OpenAI/Ollama/vLLM/gateways and Anthropic-compatible endpoints. Wired in via INTRAKTIBLE_AI_BASE_URL/_API_KEY/_MODEL/_PROVIDER (Stub stays the fallback). Verified text + structured + error paths against a mock server and end-to-end on the real binary. | fixed`
- `D22 | eventlog | crash safety: WAL.load now recovers a torn final record (a crash mid-Append leaves a trailing line with no newline) by truncating it on reopen, instead of failing to open and bricking the log. No acknowledged event is dropped (Append fsyncs before returning), and a corrupt complete record mid-file still fails loudly. (D1's in-memory/segmentation scaling is still open.) | fixed`
- `D3 | projection | a stalled projection (a live-apply error stopped the consumer) is now surfaced: GET /healthz returns 503 "degraded" with the error when the runtime's Err() is set, so an orchestrator can restart/depool the node (a fresh boot rebuilds the projections). Dead-lettering/auto-skip is intentionally NOT added — silently dropping an event would violate fail-loudly and leave projections inconsistent. | fixed`
- `D2 | store | added a durable SQLite projection store (store.NewSQLite, pure-Go modernc.org/sqlite — no CGO) behind the existing store.Store interface, selectable with serve --store=sqlite (persists to <data-dir>/projections.db, WAL + busy_timeout for one writer / many readers). Verified data survives a restart. Postgres adapter split out as D21. | fixed`
- `D7 | auth | added a login flow: POST /v1/login exchanges a valid API key for an HttpOnly session cookie (the Authenticate middleware already accepted it), POST /v1/logout revokes it, GET /v1/me returns the caller; sessions now expire (DefaultSessionTTL) and can be revoked. Remaining follow-ups split out as D20. | fixed`
- `D8 | projection | rebuild is now idempotent: the Projector interface gained Collections(), and RebuildTo resets each projector's collections before replaying — so rebuilding into a non-empty store (a durable store, or a repeated replay) no longer double-applies. Verified by replaying the same store twice (counts unchanged). | fixed`
- `D4 | decision-engine | decide input is now validated against the version's input_schema before the run is recorded — a contract violation is a 400, not a recorded decision. Pure domain.ValidateInput enforces a JSON-Schema subset (object type / required / per-property type) with no new dependency; the unenforced keywords are tracked as D19. | fixed`
- `D6 | web | the production SvelteKit build now embeds and serves correctly: web.Handler does SPA fallback (a real embedded file is served as-is; any other path returns index.html 200) so client-side routes like /engine and /cases/{id} work from the binary. make dist (web + build) and the Dockerfile produce the single self-contained artifact; a fresh checkout still embeds the committed placeholder so go build always works, and the build output is gitignored. | fixed`

Type-safety & correctness-by-design sweep (post-launch; audit-driven)
- `TS1 | platform/mo | Option[T] + Result[T] (Some/None/Ok/Err, IsSome/IsOk, Get bridges to idiomatic (T,bool)/(T,err), OrElse/Ptr, free-function Map/AndThen). The foundation for replacing none/null sentinels where they are easy to mishandle; fully unit-tested. | done — PR #19`
- `TS2 | decision-engine/models | real bugs: Provider.Predict now Validate()s the spec before Evaluate (a malformed GBM spec seeded outside the command path errored as a nil-deref in (*Tree).eval — now a clean error), and the registry projector propagates a ParseSpec error instead of writing a kind-less ModelView row. | done — PR #19`
- `TS3 | web | real bugs: SSE/WS chunk parsing guarded (a malformed frame fails the stream loudly instead of hanging the button), bulk case ops use Promise.allSettled with partial-success reporting, builder seeds from flow.latest (not versions.at(-1)), policies render/publish guards, audit loading skeleton, CommentThread reactive reload, preapprovals double-submit guard. | done — PR #19`
- `TS4 | decision-engine | decide error taxonomy: ErrBadRequest / ErrNotFound so the HTTP layer maps cause→status (400/404/500) instead of flattening to 400. | done — PR #19`
- `TS5 | decision-engine/domain | publish-time flow validation: ValidateFlow decodes every node's config (the executor's strict decode) and dry-compiles every expr-lang expression + Code-node Starlark (pure, no I/O, no ref resolution), wired into PublishVersion/ImportFlow + the copilot generate path — a semantically-broken flow is rejected at publish, not on the first prod decision. Canonical etag (sorted-key/normalized config) so a semantically identical re-import is still a no-op republish. | done — PR #20`
- `TS6 | web | numeric input validation: a non-numeric drift threshold / feature window surfaces an error instead of silently coercing to 0. | done — PR #20`
- `TS7 | platform/store+projection | type-enforced the load-bearing crash-safety invariant: a new store.Ephemeral marker; projection.New requires its store to be either a TxStore (atomic checkpoint) or Ephemeral (rebuilt on restart), panicking otherwise — a future durable-but-non-transactional store fails loudly at startup instead of silently double-counting projector counters on crash recovery. | done — PR #21`
- `TS8 | (cross-module) | named enum types replacing bare strings (the ModelKind pattern, a Valid() method, JSON wire-compatible, event/read-model wire kept string at the boundary): ModelKind, CaseStatus, RunStatus, ConnectorType, Environment, Disposition. | done — PRs #19/#21`
- `TS9 | platform | identity.New validated smart constructor adopted at the SSO (OIDC) mint boundary so a malformed tenancy/actor fails at login, not at the first handler; inline preapprovals revoke (a11y) replacing window.prompt. Deliberately did NOT unexport Identity/command fields codebase-wide (churns hundreds of trusted internal/test literals for no gain over the Valid()-then-trust already in place), nor convert every idiomatic (T,ok) lookup to mo (targeted adoption — KMS.FromEnv → Option — is the right dose), nor the typed PreResolved effect seam (would change DecisionStarted.Data's recorded shape → break replay of existing decisions without a migration). | done — PR #21`

Observability + breadth + streaming (post-consistency)
- `TS10 | platform/metrics | Prometheus instrumentation: a private registry + /metrics endpoint (unauthenticated like /healthz, aggregate counters only). HTTP request count + latency histogram keyed by the matched ServeMux route pattern (low cardinality) via an httpx.Metrics middleware; projection_applied_seq gauge + projection_errors_total (hooked into the runtime's setApplied/setErr); scheduler_ticks_total{scheduler,outcome} from the flow-monitor + model-drift Run loops; Go runtime + process collectors. prometheus/client_golang clears vuln + licenses. Tested. | done — PR #23`
- `TS11 | context-layer/connectors | OAuth2 client-credentials auth: http/graphql connectors gain auth type "oauth2" — a token is fetched from token_url over the egress-guarded client, cached by its expiry (30s margin), and sent as Authorization: Bearer. Unlocks most OAuth2-protected enterprise REST/GraphQL APIs; client_secret sealed at rest; validated at define time. No determinism cost (connectors are recorded effects). The auth path moved apply()→authorize(ctx,req,client). Catalog rest-oauth2 template. White-box tests (grant + token caching + validation). | done — PR #23`
- `TS12 | agent-manager | agent-run token streaming: already implemented (ai.StreamingProvider.Stream on the HTTP provider; command.StreamRun → agents.InvokeStream streams deltas via onChunk and records the terminal run; SSE GET …/run/stream + WS …/run/ws endpoints; web agents page consumes it). This pass added the missing agent-level test coverage (StreamRun streams + records, for a real StreamingProvider and the Stub single-chunk fallback; unknown-agent rejection) and corrected the stale D17 note. Replay-stable: the recorded terminal event is the source of truth, streaming is delivery only. | done — PR #23`

Bug + fuzz + UI/UX sweep (audit-driven; one fat PR)
- `BF1 | HIGH | decision-engine/models | bucket() panicked on a NaN/±Inf/out-of-range probability (int(NaN) is a huge negative) — a malformed external-model probability was a poison NodeEvaluated event that crashed the DriftProjector on every replay/rebuild. bucket() is now total over any float; predictExternal rejects a non-finite score / out-of-[0,1] probability at the source. Tests added. | fixed`
- `BF2 | HIGH | context-layer/connectors | the OAuth2 token-cache key omitted client_secret → a rotated secret kept serving the stale token, and two connectors sharing token_url/client_id but different secrets could share a token. Key now includes a client_secret fingerprint; expired entries evicted on write. Test added. | fixed`
- `BF3 | HIGH | agent-manager | the agent run counter (AgentView.Runs) double-incremented when a run recorded a second terminal AgentRunRecorded (the RecoverRunning re-run path) — inflating the count and breaking replay-stability. Now bumps once per RunID (first terminal recording only). Test added. | fixed`
- `BF4 | MED | decision-engine/notify | Deliver returned nil error even when every webhook 5xx'd, so a scheduler recorded the firing-edge alert and deduped it into silence (never notified, never retried). Deliver now errors on total delivery failure (partial success / no webhooks still succeed). Test added. | fixed`
- `BF5 | MED | agent-manager | the SSE stream wrote 200 + event-stream headers before attempting the run, so an unknown agent returned 200 with an error frame (and the metrics middleware counted it as success). It now resolves the agent first → real 404. Tool-loop step cap made exact (was one extra provider call). | fixed`
- `BF6 | MED | context-layer/connectors | the egress client now refuses cross-host redirects — net/http only strips Authorization/Cookie, so a connector's custom X-Api-Key / OAuth2 bearer would otherwise leak to a redirect target on a different host. | fixed`
- `BF7 | MED | context-layer/connectors | the SQL connector silently bound a declared :arg absent from params to NULL (wrong/empty rows); it now fails loudly. The GraphQL connector now requires a {data,errors} object (a bare array/scalar response no longer passes as success). | fixed`
- `BF8 | MED | cmd | module gating used a non-trimming splitter, so --modules="a, b" (or a trailing comma) silently left a module unmounted; it now uses the trimming splitCSV. | fixed`
- `BF9 | LOW | decision-engine/domain | decision-table COLLECT min/max over zero matched values injected a silent null; it now fails loudly. | fixed`
- `BF10 | — | (fuzz) | added native Go fuzz targets for the parse/validate boundaries (JSON-Schema validator incl. $ref cycles, publish-time flow validation incl. expr-lang/Starlark compile, model-spec parse/validate, connector config). ~15M/6M/1.2M execs found NO crashers. Known limitation (noted, not fixed): expression EVALUATION (expr.Run, runtime) over an operator-authored expression can be pathologically slow — the pure deterministic core can't hold a wall-clock timeout, so it's an unbounded-evaluation tradeoff, not a parse defect (publish only compiles, which fuzzed clean). | done`
- `BF11 | MED | web/engine | the flows list (/engine) rendered every flow unbounded (a ~10k-px page with hundreds of flows — caught by the screenshot review); it now has a search box + a 100-row render cap with a count. | fixed`

Bug sweep round 2 + type-strengthening + fuzz (one fat PR)
- `BF12 | CRITICAL | decision-engine | REGRESSION from BF4: once notify.Deliver errored on total webhook failure, both monitor.Scheduler.Tick and models.Scheduler.Tick `return`ed on the first failing tenant/model — one tenant's down webhook starved every tenant/model processed after it (alerting silenced fleet-wide), repeating every tick. Now a delivery failure skips marking THAT one alerted (re-delivers next tick) and continues the sweep (DeliveryFailures counter); the on-demand /monitors/check reports per-webhook delivery outcomes + a delivery_failed flag instead of 500-ing a successful evaluation. Regression test added. | fixed`
- `BF13 | HIGH | platform/scim | a SCIM Create that omitted "active" decoded it to false and provisioned a deactivated (locked-out) user; per RFC 7643 omitted = true. Now probes presence (*bool) and defaults to active. Test added. | fixed`
- `BF14 | MED | web/engine | the flows-list search (added with the render cap) didn't match flow_id, so a flow past the 100-row cap was unreachable; flow_id is now searchable. | fixed`
- `BF15 | MED | decision-engine/export | BPMN edge endpoints not present among the nodes bypassed the id-uniqueness map and could collide with a suffixed node id (cross-wiring sequence-flow refs); edge endpoints now go through the same uniqueness set. FuzzBPMN added (9.6M execs, no crashers). | fixed`
- `BF16 | LOW | platform/erasure | the already-sealed check matched any object carrying $intraktible_erased; it now requires exactly the {$intraktible_erased, value} shape, so real PII carrying a field of that name isn't mistaken for sealed and left in the clear. | fixed`
- `TS13 | — | decision-engine/models | Spec.Validate rejects a field belonging to a DIFFERENT kind (stale endpoint on a logistic spec, etc.), so flipping kind + leaving dead config fails loudly. Test added. | fixed`
- `TS14 | MED(security) | platform/auth | Scope.Allows("") now fails closed (grants nothing) instead of treating an empty scope as unrestricted — creation defaults empty→Sandbox, so an empty scope at check time means corruption/legacy; fail-open was a privilege-escalation hole. Test updated. | fixed`
- `BF-note | history rewrite | the stray 52 MB root `intraktible` binary (from BF/PR #24) was purged from ALL git history via git-filter-repo + force-push (main rewritten; .git 29M→1.7M; backup bundle retained). | done`

Bug sweep round 3 + type-strengthening + fuzz (audit-driven; one fat PR)
- `BF17 | CRITICAL(security) | platform/httpx+auth | privilege escalation: POST /v1/login exchanged an API key for a session but DROPPED the key's scope (Issue took only id+role), and the session auth branch never set a scope, so allowEnv treated "no scope" as unrestricted — a sandbox-scoped key → login → session → call production. Sessions now carry scope (Issue/Resolve + storedSession), both auth branches set ONE httpx.Principal{Role,Scope} value (role-without-scope is now unrepresentable), SSO sessions mint as ScopeAll (human builder, all envs), and allowEnv fails closed (missing scope = 403). Regression + round-trip tests added. | fixed`
- `BF18 | CRITICAL(security) | cmd/intraktible | the dev admin key (ScopeAll+RoleAdmin) was seeded by DEFAULT, guarded only by a log line — a production deploy that didn't pass --dev-api-key="" booted with a known admin credential. It is now seeded ONLY with the in-memory store (the dev/test default); any durable store (sqlite/postgres — the only kind a real deployment uses) refuses to seed it regardless of the flag. Extracted testable seedDevKey + test. | fixed`
- `BF19 | CRITICAL | decision-engine/notify | a webhook returning a permanent 4xx (404/410 — gone for good) made Deliver error → the scheduler never recorded the firing-edge alert → it re-delivered to the dead endpoint EVERY tick forever, alert never deduped. Introduced an Outcome enum (Accepted|Retryable|Permanent): Deliver signals a retry ONLY for retryable failures; all-permanent returns nil so the alert edge records (recorded in the audit trail either way). Kills the "non-retryable treated as retryable" class via the type. Permanent-failure + outcome tests added. | fixed`
- `BF20 | HIGH(security) | platform/httpx+auth | a caller (admin-gated) could mint OR rotate a key broader than their OWN credential — a sandbox-scoped admin issuing a production key, or rotating an existing production key into a fresh live secret. Added a Scope.Covers lattice; create + rotate now enforce the caller's scope/role as a ceiling. Ceiling-rejection + Covers tests added. | fixed`
- `BF21 | HIGH | platform/scim | only Create bounded its body; every other SCIM mutation (patch/createGroup/replaceGroup/patchGroup) read r.Body unbounded behind only the static bearer token (OOM lever). All routed through one bounded decodeSCIM helper (1 MiB). Oversized-body test added. | fixed`
- `BF22 | HIGH | decision-engine/service | decideBatch recorded an infrastructure (500-class) Decide error as a per-row "rejected" under an overall 200 — a DB outage mid-batch looked like permanent client rejections. It now branches on the error cause (decideStatus): client errors reject the row, infra errors abort the batch (500). The NDJSON stream path emits a terminal error line + stops rather than mislabeling. decideStatus mapping test added. | fixed`
- `BF23 | HIGH | agent-manager | the tool-calling loop never checked ctx between steps — a cancelled/disconnected request kept running up to maxToolSteps billable provider+tool rounds. Now honors ctx.Err() each iteration. Also, when the model requested a tool the agent doesn't declare, the attempted calls were dropped from the run trace; they're now recorded. SSE stream now aborts the run on a write failure (client gone); the WS opening read is deadline-bounded (no goroutine pinned by a silent client). Cancellation test added. | fixed`
- `BF24 | HIGH | web/engine | the [flowId] $effect launched 7 un-cancelled loaders on every flow change; a slow response for the PREVIOUS flow could resolve after sibling-nav and clobber the new flow's editor/canvas/metrics (last-write-wins race). Each flow-scoped loader now captures the requested flowId and drops its result if flowId changed mid-flight. | fixed`
- `BF25 | HIGH | decision-engine/export | BPMN drew DI waypoints for dangling edges off a zero box (invalid BPMN: a sequenceFlow/shape referencing a missing element), and mermaidID coerced distinct ids (a.b / a/b) to the same identifier — merging nodes and cross-wiring edges. Added shared soundEdges (drop edges to undeclared nodes, used by BPMN/Mermaid/DOT) + generic assignIDs (per-export uniqueness, used by BPMN+Mermaid). FuzzMermaid + collision/dangling tests added. | fixed`
- `TS15 | — | platform/httpx | the auth principal is now ONE Principal{Role,Scope} context value set by both auth paths, replacing two independently-set context keys — a request that carries a role but no scope is no longer representable (the root cause of BF17). | done`
- `TS16 | — | decision-engine/notify | DeliveryResult.Outcome (Accepted|Retryable|Permanent) replaces a bare ok bool, making the schedulers' retry/dedup decision total over a closed set (root cause of BF19). | done`
- `TS17 | — | web/nodevis | a single NodeType union is the source of truth for the palette (NODE_TYPES), the accent map, and nodeSummary's exhaustive switch (never default) — a new node type that isn't handled everywhere now fails the type-check instead of silently rendering a neutral fallback (surfaced + fixed the predict-node omission; added --node-predict). | done`
- `BF26 | MED | decision-engine/policy | ActiveForFlow (decide hot path) + the policy backtest resolved the spec via Versions[len-1] instead of the tracked Latest — Versions is appended in event order, not version order, so the wrong spec could silently run. Centralized in latestVersion(View) (select by Latest). Test added. | fixed`
- `BF27 | MED | decision-engine/monitor | Define accepted a NaN/±Inf threshold — a silent dead monitor that compares false against every value and never fires. Rejected at the write boundary. Test added. | fixed`
- `BF28 | MED | decision-engine/assertions | a subset assertion expecting a field to be null PASSED vacuously when the output omitted the field (got[k] is nil for both absent and explicit null) — false confidence. Now distinguishes presence. Test added. | fixed`
- `BF29 | MED | decision-engine/analytics | AvgDurationMS used integer-truncating division (systematically biased low) behind an always-true guard; now rounds to nearest ms. | fixed`
- `BF30 | LOW | decision-engine/preapproval | ValidDays was unbounded above, so an absurd value overflowed the int64 ns horizon to a negative/garbage ValidUntil; capped at 3650 days. Test added. | fixed`
- `BF31 | LOW | decision-engine/models | drift read the prediction probability via a bare float64 assertion; routed through the existing toFloat (handles json.Number) so a UseNumber-decoded probability isn't silently dropped from the histogram. | fixed`
- `BF32 | — | (fuzz) | added FuzzMermaid (export: no panic on arbitrary graphs; covers id collisions + dangling edges) and FuzzUserNameFilter + FuzzParseBool (SCIM untrusted IdP filter/patch parsing — no panic, extracted userName is a genuine substring). ~4.8M/0.8M/0.5M execs, no crashers. | done`
- `note | NOT bugs (verified) | the JSON walkers (secrets/erasure/privacy) need no recursion-depth guard — encoding/json caps nesting at 10000 before they see the data, well within the stack. The managed-key hash index is not cleaned on revoke/rotate, but validate is authoritative (revoked/rotated-away hashes are rejected) and key IDs are random (no reuse), so it is harmless hygiene, not a bug. shadow's !=-reset on a changed ShadowVersion is correct for a single-writer ordered log (deliberate reassignment is a new comparison). | n/a`

Bug sweep round 4 + type-strengthening + fuzz (audit-driven; one fat PR)
- `BF33 | CRITICAL | decision-engine/domain | Execute panicked (slice bounds [1:0]) on a Decision Table with hit:"any" and ZERO matching rows — matched[1:] on an empty slice, with the len>0 guard placed AFTER the ANY block. The pure core is contractually panic-free; this crashed the decide path (dangling started-decision, 500) and any backtest/shadow batch containing such a row. Now iterates the full matched slice (empty → zero iterations). Regression test added. | fixed`
- `BF34 | HIGH | web | the BF24 stale-load guard was never propagated: the decisions/[id], cases/[id], data/[type]/[id], and agents/[name] detail pages (and CommentThread) launched load() from a $effect with no in-flight guard, so a slow response for the PREVIOUS record could resolve after sibling navigation and clobber the one now shown. Each flow-scoped loader now captures the requested id and drops a stale result. | fixed`
- `BF35 | HIGH | platform/comments+notifications | List sorted by time.Time alone with sort.Slice (unstable), so same-instant items (coarse clocks, batch import) came back in run-to-run-varying order. Added a monotonic Seq (the event-log sequence) to each View and a SliceStable tiebreak. | fixed`
- `BF36 | MED | platform/ai | the HTTP provider decoded the response body BEFORE checking the status, so a non-JSON error body (a gateway/proxy 502 returning HTML) surfaced as a misleading "invalid character '<'" decode error instead of "provider status 502". Status is now checked first; the error body is decoded only opportunistically. Test added. | fixed`
- `BF37 | MED(scaling) | platform/store | ListDocs called Store.List which returned the ENTIRE collection (all tenants) and filtered by key prefix in Go — every tenant-scoped listing (cases, decisions, notifications, …) scanned and materialized every tenant's rows. Pushed the key prefix into Store.List as an indexed range scan (key >= prefix AND key < prefixUpperBound) across memory/sqlite/postgres (+ tx), keeping the Go-side prefix filter as a correctness backstop. Tenant isolation was already correct (filtered before return); this is the scaling fix. Prefix test + FuzzPrefixUpperBound added. | fixed`
- `BF38 | MED | agent-manager | BF23 made the SSE stream abort the run on a client-gone write failure but left the WS path ignoring its write error — a mid-run WS disconnect kept producing provider chunks into a dead socket. The WS onChunk now cancels the run ctx on a write failure, mirroring SSE. | fixed`
- `BF39 | MED(security-hygiene) | platform/httpx | api-key rotate gained a scope ceiling (BF20) but revoke did not — a sandbox-scoped admin could DISABLE a production credential (a DoS against an environment they can't reach). revoke now applies the same Scope.Covers ceiling as rotate. | fixed`
- `BF40 | LOW | context-layer/domain | the feature window was documented (now-window, now] but implemented [now-window, now] (Before(cutoff) includes an event exactly on the cutoff instant), a boundary determinism mismatch. Code now matches the documented exclusive lower bound. | fixed`
- `TS18 | — | case-manager | domain.ParseStatus(string) (CaseStatus, ok) — projectors (applyStatus + the SLA fold) parse status at the event-decode boundary and SKIP an unknown value instead of casting it straight into the read model (or halting the projection on a poison event). Makes "an unknown status in the read model" unrepresentable via the boundary. Test added. | done`
- `TS19 | — | decision-engine/domain | the Decision Table hit policy + collect aggregate are now validated at PUBLISH (ValidateFlow) against the known set, so a typo (hit:"anyy", aggregate:"avg") fails when the flow is published rather than as a runtime "unknown hit policy" on the first production decision. Tests added. | done`
- `TS20 | — | platform/store | Store.List(ctx, collection, keyPrefix) carries the tenant key prefix in its signature, so a listing is scoped at the type/interface boundary rather than relying on every caller to filter after a full scan (the BF37 fix). | done`
- `note | NOT bugs / documented (verified) | (1) Postgres multi-node HA: BIGSERIAL is assigned at INSERT but visible at COMMIT, so under concurrent multi-node appends the delivery poller can advance its watermark past a lower seq that commits late, skipping it from LIVE bus delivery (still durably stored; returned by any full Read/rebuild — a live-latency gap, not data loss). The correct fix gates the poller on the pg_snapshot_xmin visibility horizon; deliberately NOT shipped untested (needs a live multi-node Postgres to validate; a naive contiguous watermark would deadlock on seqs burned by rollbacks). Documented in postgres.go. Single-node (default file/sqlite/memory) is unaffected. (2) injectPredictions' "model" key is a RESERVED attribution key that intentionally overrides any provider-returned "model" (drift attribution must not be spoofable); comment clarified. (3) case status transitions are intentionally permissive (reopening a completed case is a legitimate operational flow); a hard transition table would itself break valid reopens. (4) comment reply parent-existence and @-mention recipient-existence are not validated on the write path — doing so would require a consistent read the write side lacks and risk rejecting legitimate fast replies under eventual consistency; the practical impact is cosmetic (an orphan reply renders flat; a ghost notification only lists if someone authenticates as that ghost). | n/a`

Bug sweep round 5 + type-strengthening + fuzz (audit-driven; one fat PR)
- `BF42 | CRITICAL (round-4 regression) | platform/store | the BF37/TS20 prefix range scan (key < prefixUpperBound, a byte-incremented successor) is correct ONLY under byte ordering. The Postgres docs.key column inherits the database's DEFAULT collation, which on stock images is linguistic (en_US.UTF-8); under that collation the byte-successor bound sorts differently and the range SILENTLY EXCLUDES valid rows — the notifications inbox + comment threads (':'-prefixed keys) and tenant listings ('/'-prefixed) return empty/short. The Go-side HasPrefix backstop cannot recover an excluded row (it only drops over-matches). Fixed by pinning every key comparison + ORDER BY to COLLATE "C" (byte order) and adding a C-collation index docs_collection_key_c so the range stays index-backed regardless of DB collation. SQLite/memory are byte-ordered already. Postgres prefix test added (runs against a live DB) + FuzzMemoryListPrefix exercises the full List contract. | fixed`
- `BF43 | HIGH (security) | agent-manager | the tool-calling loop sent the provider only the agent's declared tools but then EXECUTED every tool name in the provider response without re-checking the allowlist — a buggy/prompt-injected model could drive the toolbox to invoke ANY connector in the caller's tenant (capability-confinement bypass; BF23 only handled the declares-no-tools case). appendToolResults now gates execution on the resolved-tool set, recording an undeclared call as a refusal fed back to the model instead of calling it. Test added. | fixed`
- `BF44 | HIGH (privacy) | decision-engine/command | emitEscalations copied company_name/case_type from the UNSEALED run output into ManualReviewRequested while every other recorded surface (input, node outputs, escalation Context) was sealed — so a tenant that configures those as PII left them readable after a crypto-shred. The labels are now derived from the SEALED node output (labelFromSealed): a plain string passes through, a sealed field becomes a "[sealed]" placeholder, so cleartext PII never lands in the escalation. Test added. | fixed`
- `BF45 | HIGH | web/engine | loadTelemetry (the post-test-run canvas painter) had a ~1s retry loop with no flowId guard, so sibling navigation painted the previous flow's node outputs onto the new flow's canvas — the BF24 stale-async class, not propagated to the telemetry poll. Now captures flowId and abandons on navigation. | fixed`
- `BF46 | HIGH | web/cases | the bulk-action partial-failure message was assigned to `error`, then immediately wiped by the `load()` reload (which resets error) — the user never saw which cases failed. Now reported via toast.error (survives the reload, symmetric with the success toast). | fixed`
- `BF47 | MED | web | three more async/interaction races: the login form had no double-submit guard (rapid Enter fired concurrent logins + gotos); the decisions pager only lower-clamped, so a double-click on Next before the in-flight load resolved overshot past the last page; the models drift-window selector had no request token, so the last response to ARRIVE won (not the last requested). All three now guard (busy flag / range+loading check / captured request token). | fixed`
- `BF48 | LOW (security) | context-layer/connectors | the SQLite connector's allowed-directory containment used a lexical filepath.Rel, so a symlink INSIDE the allowed dir pointing outside it passed the check while sql.Open followed it to an arbitrary file (cross-tenant DB / secrets). Now resolves symlinks (EvalSymlinks; parent-dir resolution when the file doesn't exist yet) before the containment check. Symlink-escape test added. | fixed`
- `BF49 | LOW (security) | platform/httpx | Download interpolated the filename into the quoted Content-Disposition header with no escaping — header injection / response splitting if a caller ever passed user text (today's callers use fixed/validated names). Now sanitizes CR/LF/quote/backslash/control bytes. Test added. | fixed`
- `TS21 | — | agent-manager | the resolved-tool allowlist set gates the execution path (appendToolResults), not just the outbound spec list — the declared-tool set is now the capability boundary on BOTH sides, so executing an undeclared tool (BF43) is structurally prevented. | done`
- `TS22 | — | platform/store | COLLATE "C" is pinned at the schema/query boundary (column index + every key comparison + ORDER BY), so the prefix range's byte-order assumption (prefixUpperBound) is true by construction on every backend rather than depending on the deployment's DB collation (BF42). Also aligns Postgres key ordering with the byte-ordered SQLite/memory backends. | done`
- `note | NOT bugs (verified) | the WS terminal (done/error) frame written after a chunk-write failure uses the now-cancelled ctx and is dropped — but that path fires precisely because the client already disconnected (the chunk write failed), so dropping the frame is correct, not a bug. The managed-key hash index still isn't pruned on rotate(grace=0) (harmless; validate is authoritative). | n/a`
- `note | type-strengthening deferred with rationale | the web bare-string status/disposition/op fields → discriminated unions (+ assertNever) remains a recommended follow-up: it is a representability nicety (no actual typo'd-value bug was found — the Go ParseStatus decode boundary, TS18, already prevents an invalid status entering a read model), and applying unions across the form/template surface risks broad svelte-check churn better done as its own focused PR. | deferred`

E2E stability (dev-experience; the recurring pre-push flakiness behind rounds 3–5's `--no-verify`)
- `E1 | flaky gate | web (Playwright) | THREE compounding failure modes made the pre-push Playwright suite flake and forced --no-verify on PRs #27/#28/#29: (1) the verbose `list` reporter streamed per-test lines + failure traces into pre-commit's NON-BLOCKING pipe and overflowed it (BlockingIOError aborted the whole run → spurious cascade); (2) the backend ran via `go run`, which orphans its compiled child on teardown, leaving a stale server on :8080 that the next run (reuseExistingServer) silently reused half-dead → mass toBeVisible failures across unrelated specs; (3) the event log persisted across runs in web/.pw-data, so flows accumulated until the engine list's 100-row render cap hid a freshly-created flow → "lists and creates a flow" failed DETERMINISTICALLY once the backlog exceeded 100. Fixes: reporter → `dot` (one char/test) + an HTML report written to disk (bounded stdout, no pipe overflow); the backend webServer wipes web/.pw-data and runs a BUILT BINARY (clean teardown, no orphan) with reuseExistingServer:false (always a fresh, empty backend); the vite webServer's build output is also kept off the pipe; and retries:2 everywhere (the pre-push is a CI-equivalent gate) absorbs the inherent timing blips of a parallel browser suite over an eventually-consistent projection. The palette search test also waits for the projection before searching. Validated: two clean back-to-back runs, 70 passed / 0 failed / 0 flaky / 0 stray servers. | fixed`
- `E2 | test hardening | platform/eventlog | converted the two delivery concurrency tests (TestStopCancelsInFlightRead, TestDispatchBoundsBatchAndReArms) to testing/synctest (stable in Go 1.25+; CI runs 1.26). They now run in a fake-time bubble where synctest.Wait blocks until every goroutine is durably blocked — replacing both the channel-rendezvous (which had a lost-signal race under -race) and the wall-clock timeouts with deterministic quiescence detection. No magic timeouts, no shared-state mutex; a regression deadlocks the bubble and synctest fails it immediately with a goroutine dump. Verified 50x under -race. | done`

Type-strengthening + persona depth (follow-ups picked up after the gate-stability work)
- `TS23 | — | web | the bare-string status/disposition/op/kind/state fields in api.ts are now string-literal unions (Disposition, RunStatus, BatchStatus, Variant, MonitorOp, CaseStatus, SLAState, AgentRunStatus, ModelKind, PreApprovalStatus, DeploymentRequestStatus) mirroring the Go enums, applied to the response/model interfaces and the request bodies/handlers with fixed option sets. A typo'd or unhandled value (e.g. a `status === 'complete'` comparison or a badge interpolation) now fails svelte-check instead of silently rendering unstyled. Free-text filter inputs stay `string` (they include the "any" empty value). svelte-check clean; the five surfaced form-state vars (case status, monitor op, dispositions) were narrowed to the union. | done`
- `P1 | feature | web (persona) | persona-adaptive depth beyond nav/home/actions/terms: a typed PersonaLens — a per-persona DEFAULT FOCUS on a shared list surface (operator → open case review queue; developer → failed decision traces). It is data re-prioritisation, not a skin: the page, data, and capabilities are identical across personas; only the initial filter differs and the user can clear it. Lens values are typed as the domain enums (CaseStatus/RunStatus) so an impossible focus is unrepresentable; the decisions + cases pages seed their initial filter from personaLens(resolvePersona()). Unit-tested (lens values are real enum members; non-lensed personas get the full list). | done`

Persona lens extension + bug sweep round 6 (one fat PR)
- `P2 | feature | web (persona) | extended the lens: the decisions surface lens is now an object (status + variant + env) rather than a single status, the PRODUCT persona lands on the challenger (experiment) arm, and the decisions page gained the missing variant filter control (the API already supported variant filtering — the UI didn't). Lens dimensions are typed as the domain enums (DecisionStatus/Variant). Tests updated. | done`
- `BF50 | MED (regression from TS23) | web | Decision.status was narrowed to RunStatus ('completed'|'failed'), but the decision read model writes 'started' for an in-flight decision (history.go applyStarted) — so the union denied a value the wire can carry, and a 'started' decision rendered an unstyled badge (the exact failure TS23 set out to prevent). Added DecisionStatus = RunStatus | 'started' and applied it to Decision.status; the terminal DecideResult/BacktestOutcome/SweepPoint/AssertionResult keep RunStatus. Caught while extending the lens into the decisions surface. | fixed`
- `BF51 | HIGH | agent-manager | the tool-calling loop echoed the model's tool-call IDs verbatim into both the assistant message and the tool-result messages with no uniqueness/non-empty guarantee — a model returning multiple calls with empty or duplicate IDs breaks the assistant↔result correlation (strict OpenAI-compatible providers 400 the next request; lenient ones misattribute a result). normalizeToolCallIDs now synthesizes a unique non-empty id per turn before either use. Test added. | fixed`
- `BF52 | MED | context-layer/connectors | flattenQuery silently DROPPED array/object/null query params (only string/number/bool were encoded), so a flow passing e.g. a Stripe expand array got a different, wrong upstream request with no signal. It now fails loudly (errors naming the key + type) instead of dropping. | fixed`
- `BF53 | MED (security) | platform/scim | a userName eq "" filter parsed to "" and Store.List treated "" as "no filter" → it ENUMERATED every user in the tenant. userNameFilter now returns (value, present); a present-but-empty clause is a precise filter that matches no user (userNames are non-empty), not "list all". Tenant-scoped (no cross-tenant leak), but wrong semantics + unintended enumeration. Test added. | fixed`
- `BF54 | LOW | platform/scim + decision-engine | SCIM Store.List now sorts by id (deterministic ListResponse across backends). And the monitor/model schedulers count Delivered only when an endpoint actually accepted (notify.AnyAccepted) — an all-permanent-failure sweep correctly dedups via a nil return but delivered nothing, so it no longer inflates the delivered metric. Tests added. | fixed`
- `note | NOT fixed / documented (verified, deliberate) | (1) the monitor/model scheduler reads the dedup flag (Alerting) from the projection of the Alerted event it just appended; if a tick somehow ran within the projection's (ms) apply latency it could re-fire, but the tick interval is minutes — fixing it needs cross-tick scheduler state that risks more than the minutes-vs-ms window warrants; documented. (2) labelFromSealed coerces a non-string manual_review label to "[sealed]" — only reachable via a hand-crafted event since evalManualReview enforces string upstream. (3) DEFERRED type-strengthening: notify.Deliver's error-as-control-flow → a typed DeliverySummary{Accepted,Retryable,Permanent}, and the decision-engine domain status string consts → a type RunStatus string with Valid() (mirroring case-manager's CaseStatus). Both are refactors of correctly-working code, better isolated to their own PR than bundled here. | n/a`

Type-strengthening + persona depth + bug sweep round 7 (one fat PR)
- `TS24 | — | decision-engine/notify | the two DEFERRED type-strengthenings landed. notify.Deliver now returns a typed DeliverySummary{Results, Accepted, Retryable, Permanent} with RetryWorthy()/Delivered() predicates instead of error-as-control-flow (a sentinel error that meant "retry"); a real failure (store/marshal) is now distinct from "every webhook is a retryable failure", and the schedulers + check handler branch on typed state. Removes the old AnyAccepted helper. Tests updated. | done`
- `TS25 | — | decision-engine/domain | RunStatus is now a named type (type RunStatus string + Valid(), mirroring case-manager's CaseStatus) on domain.Run.Status and its consts, so the decision core can't carry a typo'd bare-string status; it stays JSON-wire-compatible and converts to string only at the event/DecideResult wire boundary. | done`
- `P3 | feature | web (persona) | persona depth gained an ORDER dimension: the cases lens is now {status, sort} and the operator lands on the open queue ORDERED BY URGENCY (soonest-due/overdue first, days_left ascending). Same data, re-ordered for the viewer — complementary to the filter lens. Typed CaseSort; unit-tested. | done`
- `BF55 | CRITICAL | web/engine | a node/edge deleted on the CANVAS (Backspace/Delete) was removed from SvelteFlow's bound nodes/edges but NOT folded back into editNodes/editEdges (the publish source of truth) — so it reappeared on the next syncCanvas() and, worse, got RE-PUBLISHED. Added an ondelete handler that reconciles the model (the inverse of onConnect's add-reconciliation). | fixed`
- `BF56 | MED | decision-engine/export | a predict node (external ML inference, like ai/connect) fell through to the generic task/rectangle in all three exporters (BPMN/Mermaid/DOT) instead of a service-task/cylinder — a silent semantic downgrade in every exported diagram. Added it to the service arm in all three. Test added. | fixed`
- `BF57 | MED | web/data | the feature-window guard allowed an empty field: Number('') is 0, which passed the >=0 check, silently defining a 0-hour feature that never aggregates. Now rejects empty + requires >=1 hour. | fixed`
- `BF58 | MED | web/decisions | the filtered list load had no generation token (rapid Apply / pager overlap could clobber with a stale response) and Apply wasn't disabled in-flight. Added a loadSeq token (only the latest response writes) + disabled Apply while loading. | fixed`
- `BF59 | LOW | web/policies | the backtest dataset JSON.parse was as-cast to an array without checking — a non-array (e.g. {}) sent a malformed dataset. Added an Array.isArray guard that errors. | fixed`
- `BF60 | LOW | platform/eventlog | WAL Read trusted the stored Seq from the payload over the authoritative byte-offset position (the offset index + projection contiguity key off it). Now assigns the positional seq, matching SQLite/Postgres deriving Seq from the row id. | fixed`
- `BF61 | LOW | context-layer/domain | feature.Compute validated the aggregation only inside the per-event loop, so an unknown aggregation with zero matching events silently returned 0. Hoisted to a precondition. Test added. | fixed`
- `note | NOT fixed / documented (verified, deliberate) | (1) httpx.requiredRole's default RoleOperator is a deliberate policy ("runtime mutations are operator-level"); all current mutating routes are explicitly classified, so it is future-proofing (a new sensitive route must be classified), not a live hole. (2) 5xx responses returning err.Error() verbatim is the deliberate self-hosted "fail loudly" stance (the panic path is already generic). (3) the CommandPalette agent-name key is safe — agent names are the unique registry key, so duplicates can't occur. (4) CommandPalette lacks a Tab focus-trap (a11y) and a few rotate/revoke buttons lack an in-flight disable — minor, noted for a UI-polish pass. | n/a`

Type-strengthening + persona depth + bug sweep + fuzzing round 8 (one fat PR)
- `TS26 | — | context-layer/domain | feature aggregation (count|sum) is now a named type Aggregation + Valid(), the last closed enum in the context-layer domain still carried as a bare untyped const and hand-validated in three places. Eliminates the "bad aggregation silently computes 0/bypasses the switch" class; JSON-wire-identical, converts to string only at the event boundary. Existing tests still pass (untyped string literals assign to the named type). | done`
- `TS27 | — | decision-engine/preapproval | the pre-approval lifecycle status (active|revoked) is now a named type Status + Valid() on the View + consts, so a typo'd status can't silently make a valid pre-approval un-honorable at the ActiveFor check. | done`
- `TS28 | — | platform/notifications | the notification Kind ("mention") is now a named type + KindMention const rather than a bare string, keeping the (currently single) kind from drifting into a typo as sources are added. | done`
- `TS29 | — | decision-engine/domain | the DMN decision-table hit policies (first|unique|any|rule_order|collect) are now an internal named type hitPolicy, so the runtime resolution switch AND the publish-time validateHitAggregate switch range over typed constants — a mistyped policy fails to compile rather than falling through to a runtime error. No wire change (normalized from raw JSON as before). | done`
- `TS30 | — | agent-manager | the AgentRunRecorded projector now parse-guards the run status at the decode boundary (domain.ParseRunStatus, mirroring case-manager's ParseStatus) instead of blindly casting RunStatus(p.Status); an unknown/legacy value records as RunFailed (safe terminal) rather than landing verbatim where SummarizeRuns would miscount it. Test added. | done`
- `TS31 | — | web | added a real assertNever helper (api.ts had referenced one that didn't exist; nodevis's exhaustive node-type switch now uses it) and an Environment union ('sandbox'|'staging'|'production'). Retyped Decision/DeploymentRequest/DeployInput.environment, Monitor/FiredMonitor metric+op (to the existing MonitorMetric/MonitorOp unions), EditNode.type → NodeType, and the decisions/cases filter state to the domain unions. The production four-eyes gate (=== 'production') is now type-checked: a mistyped env literal that would silently disable maker-checker fails svelte-check. Wire boundaries `as`-cast as before. svelte-check clean. | done`
- `TS32 | — | case-manager/domain | CaseStatus gained a Terminal()/CanTransitionTo() state machine, so "mutate a closed case" is rejected by the type's own method wherever a transition is applied (see BF62) rather than re-checked ad hoc. Test added. | done`
- `BF62 | HIGH | case-manager | SetStatus validated only that the target status was known and the case existed — it never folded the case's CURRENT status, so a completed (terminal) case could be moved back to in_progress/needs_review, silently re-arming the SLA sweep against a legitimately-closed case. SetStatus now folds the current status under the lock and enforces CaseStatus.CanTransitionTo (terminal → only itself, an idempotent no-op). Tests added (command + domain). | fixed`
- `BF63 | MED | decision-engine/history | List sorted decisions with an unstable single-key sort.Slice on StartedAt; two decisions at the same instant (concurrent /decide) ordered arbitrarily and differently across calls, so ListPage could skip or duplicate a record at a page boundary. Added DecisionID as a deterministic tiebreaker. | fixed`
- `BF64 | MED | context-layer/domain | feature.Compute could overflow a sum to ±Inf (or produce NaN) from extreme values and return it as a feature, where a non-finite value silently corrupts downstream rule/expression comparisons. Now fails loudly on a non-finite result. Surfaced and covered by FuzzFeatureCompute. | fixed`
- `BF65 | LOW (security) | platform/httpx | DecodeJSON decoded only the first JSON value and ignored trailing data, so a body like `{"role":"viewer"}{"role":"admin"}` silently used only the first object — weakening the strict-decode guarantee. Now rejects trailing data (dec.More()). Test added. | fixed`
- `BF66 | LOW (security) | platform/httpx | the OIDC state and SAML RelayState single-use CSRF tokens were compared with != ; now compared in constant time (subtle.ConstantTimeCompare via a shared secureEqual helper) per the secret-comparison rule. | fixed`
- `BF67 | LOW | decision-engine/models | applyBaseline used UpdateDoc (a no-op when the model's stats doc doesn't exist yet), so a baseline captured before the model's first prediction was silently dropped and lost on replay — unlike its sibling applyMonitorSet which seeds the doc. Now seeds when absent, mirroring the sibling. | fixed`
- `BF68 | HIGH | web/cases | the cases list load had no generation token (unlike decisions), so rapid status-filter changes / Reload could resolve out of order and leave rows that no longer match the selected status. Added a loadSeq token (only the latest response writes). | fixed`
- `BF69 | MED | web/data | the data page load had no generation token and Reload had no in-flight disable — repeated Reload (or Reload during a define follow-up load) could clobber the tables out of order. Added loadSeq + a disabled Reload while loading. | fixed`
- `BF70 | MED | web/audit | the audit list load (driven by afterNavigate + Apply + paging) had no generation token; rapid paging/Apply could show stale rows/total. Added loadSeq. | fixed`
- `BF71 | MED | web/keys | rotate/revoke had no in-flight guard and the buttons no disable, so a double-click on Rotate minted TWO real secrets — the second overwrote `revealed`, leaving the first valid secret shown to no one and unrecoverable. Added a per-key mutating guard + disabled buttons. | fixed`
- `BF72 | LOW | web/decisions | downloadTrace revoked the blob object URL synchronously after click(), which can race the browser's fetch and abort the download (larger traces). Now revokes on a later tick. | fixed`
- `BF73 | LOW | web (preapprovals + engine) | three number inputs (preapproval/engine valid_days, monitor threshold) bound straight into the request body; a cleared input binds as null in Svelte 5, posting valid_days:null / threshold:null. Added null/integer guards (mirroring the data-page window guard). | fixed`
- `BF74 | LOW | web/models | the drift histogram bar height used Math.max(1, ...drift.hist); a single non-finite bucket (a null from the wire) makes Math.max return NaN and collapses every bar to NaN%. Extracted a guarded histBarHeight (finite buckets only). | fixed`
- `P4 | feature | web (persona) | the config-driven PersonaHome at-a-glance tiles are now persona-chosen (PersonaConfig.homeStats over a catalog computed from the SAME dashboard data): a developer sees failed / p95 latency / completion, a manager pending-approvals / needs-review / overdue, product challenger / volume / completion — instead of the one hardcoded decisions/completed/flows deck for everyone. Pure personaHomeStats helper, unit-tested; unset personas keep the default deck. | done`
- `P5 | feature | web (persona) | the decisions lens gained a `columns` dimension: the same rows lead with the columns that role debugs by — a developer's table leads status → duration → env (dropping the experiment variant), product leads with variant. Typed DecisionColumn; the table renders the persona's ordered subset, falling back to the full set. | done`
- `P6 | feature | web (persona) | the decisions + cases lenses gained role-specific empty-state copy (EmptyCopy): a developer's empty failed-traces view reads "No failing traces — your integration is clean", an operator's empty queue "The review queue is clear — every open case is within SLA", in the persona's own vocabulary rather than the generic onboarding message. | done`
- `FUZZ | test hardening | added five native fuzz harnesses at the highest-value un-fuzzed boundaries: FuzzExecute (the runtime decision path — every node evaluator against adversarial input, asserting no panic + valid terminal status; ValidateFlow was fuzzed but only dry-compiles), FuzzWAL (the on-disk log decoder — asserting OpenWAL+Read never panic and a successful Read returns contiguous seqs, so the load() offset index and Read() record split can't desync into a silent mis-read), FuzzSecretsRoundTrip (AES-GCM seal/open round-trip + adversarial open of arbitrary envelope JSON), FuzzFeatureCompute (which surfaced BF64), and FuzzDOT (the un-fuzzed Graphviz exporter's hand-rolled dotQuote escaper — balanced-quote invariant). | done`
- `note | incidental cleanup (folded in to keep the deadcode gate green) | removed two PRE-EXISTING dead functions the whole-program deadcode gate flagged (confirmed present on clean HEAD, not introduced here): case-manager domain's ValidStatus free function (the CaseStatus.Valid() method is the form in use) and decision-engine domain's Environment.Valid() method (the ValidEnvironment free function is the form in use at the wire boundary). dupl count went 9→8 (no new clones added). | n/a`

Structural type-strengthening + Go↔TS enum codegen + bug sweep + fuzzing round 9 (one fat PR)
- `TS33 | — | platform/auth | API-key usability ("is this key live") was open-coded three different ways — validate (revoked OR expired), Rotate (revoked only), and the UI. Introduced a KeyStatus type + ManagedAPIKey.Status(now)/Usable(now) as the single predicate, used by validate and Rotate. Closes the divergence class where a future caller forgets the expiry check and reopens an auth bypass. Test added (revoked-wins-over-expiry). | done`
- `TS34 | — | decision-engine/models | the GBM Spec.Link was a bare string ("" | "logit") with the evaluator branching on == "logit"; a mistyped "Logit" passed Validate and silently applied the identity link (no sigmoid). Now a named GBMLink type (LinkNone/LinkLogit) with Valid(), wire-identical so stored ModelDefined events still decode. The flat Spec wire is deliberately kept (a nested discriminated union would change the JSON shape and break replay of existing events); the kind-union invariant stays enforced by noForeignFields at construction. Test added. | done`
- `TS35 | — | agent-manager | the agent Outcome's payload (Text/Structured/Error) had to agree with Status by convention, zeroed ad hoc per terminal branch. Added a normalize() that enforces the Status⇄payload invariant once at each Invoke return (failed → only Error; completed → no Error), so "completed with an error" / "failed with output" are not observable/recordable states. Documented the invariant on the type. | done`
- `TS36 | — | web + cmd/tsenums | the TS string-literal unions that mirror Go enums 1:1 are now GENERATED from the Go consts (cmd/tsenums → web/src/lib/enums.generated.ts; `make tsenums`), making the Go enum the single source of truth so values can't drift. A Go drift-check test (cmd/tsenums) fails the gate if the committed file is stale. api.ts re-exports the 10 generated unions (Disposition/RunStatus/Environment/CaseStatus/SLAState/AgentRunStatus/ModelKind/PreApprovalStatus/MonitorOp/MonitorMetric) + ENVIRONMENTS/MONITOR_METRICS; composites (DecisionStatus/BatchStatus) and the two still carried as scattered Go literals (Variant/DeploymentRequestStatus) stay hand-defined. svelte-check + the generated file are prettier-ignored to stay byte-stable for the drift check. | done`
- `BF75 | HIGH (security) | decision-engine/command | a caller could pre-populate the engine-reserved input namespaces (features/connect/ai/predict): when a flow had no corresponding node (or no provider was wired, so the injector no-ops), the caller's value passed straight into the executed + recorded input and was read by Rule/Split/Scorecard expressions as if engine-resolved — letting a request FORGE feature values or model scores the flow author trusts. Decide now strips these reserved keys from caller input after validation, before injection, so the namespaces are engine-owned. Behavior test added (a forged "features"/"predict" never reaches the recorded input). | fixed`
- `BF76 | HIGH | web/engine | loadTelemetry's ~1s retry loop was guarded against flow NAVIGATION but not against EDITS to the same flow: editing a card during the retry window let a late retry repaint the previous run's node outputs onto the now-changed canvas. Added a telemetryGen counter (bumped by clearTelemetry, which runs on every edit) captured by the loop, abandoning on change — the flowId guard, for edits. | fixed`
- `BF77 | MED | decision-engine/domain | two Connect (or two AI, or two Predict) nodes writing the same output name silently collided — the result map is keyed by output within each namespace, so one node's result was discarded (the connector/model still called) by declaration order. ValidateFlow now rejects duplicate outputs per node-type at publish. Test added. | fixed`
- `BF78 | MED (security) | context-layer/connectors | the process-global OAuth2 client-credentials token cache was keyed by token_url/client_id/scope/secret-fp but NOT tenant: two tenants sharing those config values shared one cache entry (cross-tenant token reuse, bounded only by the secret fingerprint). The key is now tenant-scoped (org\x00workspace from the request context). | fixed`
- `BF79 | MED (security) | context-layer/connectors | header/query connector auth validated only the name, so an empty value emitted Header.Set(name, "") — a request that LOOKS authenticated but carries no credential. validate() now requires a value. | fixed`
- `BF80 | MED (security) | platform/httpx | the rotate API-key handler enforced the scope ceiling but not the role ceiling that create enforces — a latent privilege-escalation path (rotation mints a fresh live secret) if the admin-only route gating were ever relaxed. Added the role ceiling, mirroring create. | fixed`
- `BF81 | MED | web/engine | the deploy challenger-traffic % accepted any value > 0 with no upper bound (unlike the sibling number-input guards); a fat-fingered 500 was POSTed. Now rejects > 100. | fixed`
- `BF82 | MED | web/policies | selecting a different policy repopulated the band editor but left the previous policy's backtest preview-impact table (and error) rendered under it. edit() now clears btReport + error. | fixed`
- `BF83 | MED | web/cases | the case-detail notes/audit {#each} keyed by timestamp (or at+type) would throw a duplicate-key error if two entries shared a millisecond. These are wholesale-replaced display lists, so they're now keyed by index. | fixed`
- `BF84 | LOW | decision-engine/domain | a published manual_review node with a negative sla_days emitted a ManualReviewRequested already-overdue on arrival. ValidateFlow now rejects sla_days < 0 at publish. Test added. | fixed`
- `BF85 | MED | decision-engine/domain | the decision-table COLLECT reducer (aggregateValues) didn't guard non-finite inputs/results: a NaN made min/max order-dependent (every comparison with NaN is false) and a large sum could overflow to ±Inf, poisoning downstream expr evaluation. Now rejects non-finite inputs and a non-finite aggregate. Surfaced + covered by FuzzAggregateValues (order-invariance + finiteness). | fixed`
- `BF86 | LOW | web/engine | the engine-page run-trace download revoked the blob URL synchronously after click() (the BF72 class, not propagated here) — can race the browser fetch and abort larger downloads. Now revokes on a later tick. | fixed`
- `FUZZ | test hardening | added FuzzAggregateValues (decision-table reducer — order-invariance + finiteness; surfaced BF85), FuzzResolveSQLiteDSN (the security-critical SQLite DSN parser — asserts every successful resolution is read-only AND contained within the allowed dir, no `..`/symlink escape, never panics on adversarial bytes), FuzzPolicyApply (disposition evaluator over adversarial expr conditions + output maps — never panics, always a valid disposition), and FuzzMemberIDFromPath (the hand-rolled SCIM PATCH-path quote-index parser). | done`
- `note | NOT fixed / documented (verified) | the Postgres "projection wedge on a non-contiguous seq" raised by a sub-agent is NOT a bug — Read uses `seq >= fromSeq ORDER BY seq` and applyContiguous advances `applied` to the real seq, so a permanent gap (rolled-back BIGSERIAL) is skipped, not wedged. The DecryptSecrets-with-nil-keyring pass-through and the sqlite mode=ro query-param passthrough remain documented operator-trust footguns, not live holes. | n/a`

Variant + DeploymentRequestStatus named types (codegen follow-up)
- `TS37 | — | decision-engine | the last two enums still carried as scattered string literals are now named types, completing the Go→TS codegen coverage. domain.Variant (champion|challenger) replaces the literals in the decide path (resolveVersion now returns it) and analytics; flows.RequestStatus (pending|approved|rejected) replaces the literals in the deployment-request projection, the command-side fold (deployReq.status), and the service responses, and types the DeploymentRequest.Status read-model field. Both have Valid() (tested) and JSON-identical wire shapes (stored read models still decode). cmd/tsenums now generates Variant + DeploymentRequestStatus into enums.generated.ts, so api.ts re-exports all 12 enums from the single Go source and the drift-check test covers them too. | done`

Bug sweep + type-strengthening + fuzzing round 10 (one fat PR)
- `BF87 | MED (security) | context-layer/connectors | isSecretEnvelope returned true for ANY map carrying the $intraktible_sealed marker, so a credential value shaped as an object that merely carried that key was treated as already-sealed and written to the event log / config in the CLEAR (the seal step was skipped). Now requires the exact envelope shape ({$intraktible_sealed, value, [key]}), mirroring erasure.isErasedMap's strictness. Test added (an envelope-lookalike credential is still sealed). | fixed`
- `BF88 | LOW (security) | platform/httpx | the admin gate matched /v1/audit by EXACT path while api-keys/erasure used HasPrefix — a trailing-slash /v1/audit/ could fall through to the GET→viewer rule. Hardened to also cover /v1/audit/ (defense-in-depth; the /monitors|/deployments Contains checks were verified safe since path segments carry no slashes). | fixed`
- `BF89 | MED | decision-engine/notify + monitor | notify.List and monitor.ListByFlow sorted on CreatedAt alone (unstable, no tiebreaker), so two webhooks/monitors created in the same clock tick ordered arbitrarily and diverged between reads and incremental-vs-rebuilt projections — the class already fixed in comments/notifications. Added a Seq field (from the creation event) and a total-order tiebreaker, via a new generic store.SortByTime / store.ListByTime helper now shared by history, notify, and monitor (also dedups the three sort sites). | fixed`
- `BF90 | LOW | platform/eventlog | WAL.Read ignored ReadAt's returned byte count, so a file truncated under the process (short read, io.EOF with n<len) would json.Unmarshal a partly-zero buffer or silently return fewer events than Head(). Now requires the full read. | fixed`
- `BF91 | LOW | context-layer/connectors | the OAuth2 token cache captured the clock BEFORE the (multi-second) token round-trip and reused it for the new entry's expiry + the eviction sweep, truncating the effective TTL by the fetch latency. Re-reads the clock after the fetch. | fixed`
- `BF92 | MED | web/engine | downloadExport (BPMN/DOT/JSON/Mermaid flow export) revoked the blob object URL synchronously after click() — the BF72/BF86 race, not propagated here — which can abort a larger export's download. Now revokes on a later tick. | fixed`
- `BF93 | LOW | web/login | the SSO/SAML provider discovery in onMount had no .catch, so a discovery failure (SSO not configured / transient) produced an unhandled rejection. Now falls back to an empty button list — the password form is unaffected. | fixed`
- `BF94 | LOW | web/agents | a server-sent stream 'error' frame stopped the WebSocket run silently (treated like 'done'), leaving the UI stopped with partial chunks and no message. Now surfaces the error text via failStream and still reloads the recorded run. | fixed`
- `BF95 | LOW | web | NotificationsBell de-snaked only the FIRST underscore of a subject_type (replace('_',' ')), so a 3-part type rendered "manual review_case". Now replaces all (/_/g). | fixed`
- `TS38 | — | web | extended the Go→TS codegen to NodeType, Aggregation, Role, and Scope (now 16 generated unions). GraphNode.type / NodeRecord.type → NodeType (nodevis re-exports the generated NodeType, dropping its hand-maintained copy and the two engine-page casts' raison d'être); Feature.aggregation → Aggregation; ManagedApiKey/CreateApiKeyRequest scope/role → Scope/Role; DecisionFilter env/status/variant → the existing unions. The keys page's hand-duplicated ROLES/SCOPES arrays are replaced with the generated ones — exactly what the codegen exists to prevent. svelte-check clean; drift test covers the four new enums. | done`
- `TS39 | — | decision-engine + context-layer | monitor.DefineCmd.Metric/Op are now the named monitor.Metric/Op (validated via .Valid(), event keeps wire string); connectors.Template.Type is now domain.ConnectorType (the static catalog had a typo'd-type hazard); assertions compares against domain.StatusCompleted instead of the bare "completed" literal. | done`
- `FUZZ | test hardening | added four harnesses at security/recursion boundaries: FuzzMask (PII redaction — asserts redaction-COMPLETENESS at any depth + idempotency + never-panics on deep nesting), FuzzSealOpenFields (crypto-shred — seal/open round-trip is lossless + adversarial OpenFields over envelope-lookalikes never panics), FuzzStarlarkRoundTrip (Code-node JSON⇄Starlark is a fixpoint after the lossy first pass), and FuzzAuditCSV (export re-parses + every cell neutralized against spreadsheet-formula injection). No bugs surfaced — the targets are robust. | done`
- `note | NOT fixed / documented (verified, deliberate) | (1) cross-process flow version/slug TOCTOU (H1): version numbers are folded-then-appended under a per-Handler mutex — correct for the monolith, but two decision-engine PROCESSES (the split-services profile) racing PublishVersion could duplicate a version/slug. The real fix is a storage-layer compare-and-set (UNIQUE(stream,aggregate,version) + expected-head Append) — a larger change tracked with the known multi-node seq-gap limitation. (2) NATS live-delivery loss across reconnect (H2): the ephemeral DeliverNew consumer misses events appended during a disconnect; the SQLite/Postgres pollers self-heal, NATS needs a durable consumer. (3) SCIM list ignores startIndex/count pagination (M5) — large directories return in one page; an IdP-pagination feature, not a correctness bug. | n/a`
